---
title: 主动学习与 Lab-in-the-loop
status: draft
last_reviewed: 2026-08-18
tags:
  - active-learning
  - lab-in-the-loop
  - directed-evolution
lifecycle_stage:
  - screening
  - engineering
ai_task:
  - active-learning
  - experimental-design
evidence_level: prospective-experimental-validation
---

# 主动学习与 Lab-in-the-loop

## 对应的研发问题

当实验预算有限、候选空间巨大时，下一轮应该测试哪些序列，才能同时找到更好候选并让模型学得更多？主动学习和 Lab-in-the-loop 关注的是连续决策，而不只是训练一个静态预测器。

## 为什么项目内数据重要

公开抗体数据经常混合不同亲本、抗原、格式、平台和实验条件。项目内数据虽然规模较小，却通常具有：

- 相同亲本或明确的局部序列空间；
- 相同抗原和作用机制；
- 一致分子格式；
- 同一实验平台和缓冲条件；
- 可快速反馈下一轮设计。

因此，几十到几百条一致数据可能比大量异质数据更适合学习当前项目的局部适应度景观。

## 主动学习的核心

普通筛选倾向于只测试当前预测最高的候选。主动学习还考虑模型不确定性和信息增益。

一种常见选择思想是：

```text
acquisition(x) = predicted_performance(x) + β × uncertainty(x)
```

- 预测高的候选用于**利用**已有规律；
- 不确定性高的候选用于**探索**未知区域；
- `β` 控制探索与利用的平衡。

实际系统还会加入序列多样性、结构可行性、实验成本和硬风险过滤。

## 初始批次如何设计

第一轮不应只选模型最喜欢的相似序列。一个更有信息量的批次可以包含：

- 预测高分候选；
- 中等预测候选；
- 高不确定性候选；
- 不同 CDR 和突变类型；
- 随机和人工经验对照；
- 可能改善表达但不一定改善亲和力的候选；
- 必要的负样本。

这使第一轮实验同时服务于优化和学习。

## 局部模型

实验结果回来后，可以使用 one-hot 序列、PLM embedding、结构特征和能量评分训练局部模型。模型学习：

- 当前亲本哪些位置更可容忍；
- 哪类突变改善或破坏目标；
- 预测器在哪些区域不确定；
- 结合、表达和稳定性之间是否存在冲突。

模型越往后迭代，越应依赖项目实验数据修正通用先验。

## EVOLVEpro 的定位

项目资料把 EVOLVEpro 描述为 PLM 表征、少样本监督学习和逐轮实验的结合。合理定位是：

- PLM 提供初始序列表征；
- 第一轮实验建立项目局部标签；
- 监督模型把表示映射到当前表型；
- acquisition 选择下一轮候选；
- 新实验持续更新模型。

它不是“PLM 直接预测最终最优序列”，而是连接通用序列先验与项目数据的工作流。

## Lab-in-the-loop

Lab-in-the-loop 把更多环节连接成连续系统：

```text
生成或枚举候选
  -> 多属性预测与风险过滤
  -> 选择候选和对照
  -> DNA 合成、表达和实验
  -> 数据质控与入库
  -> 更新模型
  -> 下一轮设计
```

一个可靠系统还需要处理样本追踪、实验批次、失败表达、缺失值、数据版本和模型版本。它是一套计算与实验基础设施，不是只输入序列的单一软件。

## 高通量展示、FACS 与 NGS

具备展示、FACS 和 NGS 能力的团队可以：

1. 用 AI 设计聚焦文库；
2. 在多个抗原浓度和展示信号下分选；
3. 用 NGS 获得不同序列的区域分布和富集；
4. 选择代表序列测量 SPR/BLI；
5. 校准富集与真实动力学；
6. 训练结合、表达和非特异性多任务模型；
7. 设计第二代组合文库。

高通量代理标签和低通量高质量标签承担不同角色，不能互相替代。

## 多突变组合与上位性

后期优化需要从单点进入组合空间。可以先测试有信息量的单点和双点，学习哪些突变协同或冲突，再选择高阶组合。

主动学习的价值是优先测试能最大程度区分组合规律的候选，而不是穷举所有双点。

## 为什么实验量仍然不小

- 高度成熟抗体的局部景观可能平坦；
- 真正有利突变比例低；
- 多属性标签需要不同实验；
- 模型必须看到失败和边界样本；
- 上位性需要组合数据；
- 实验噪声和批次需要重复与校准。

主动学习提高信息效率，不取消建立训练集和验证集的成本。

## 评价方式

- 每轮测试候选数和实验成功率；
- 相对随机、人工和静态模型的改善；
- 达到目标需要的轮数和总实验成本；
- 不确定性是否校准；
- 是否保留候选多样性；
- 结合、表达、稳定性和功能是否同时保持；
- 完整 IgG 和目标实验中的最终结果。

## 主要失败模式

- 初始批次只包含高分相似候选；
- 把失败表达或缺失实验当作随机缺失；
- 跨轮次实验条件漂移；
- 代理标签未用真实动力学校准；
- acquisition 只最大化预测值；
- 模型和实验版本不可追溯；
- 只报告最佳候选，不报告总预算和对照。

## 关键结论

主动学习把“哪个候选最好”扩展为“哪个实验最值得做”。Lab-in-the-loop 将生成、预测、实验和数据反馈组织成持续系统。它的价值取决于稳定实验、严格数据管理和与真实研发终点一致的评价。

## 延伸阅读

- [筛选与命中确认](../../part-02-discovery-and-early-research/04-library-display-and-screening/screening-and-hit-confirmation.md)
- [抗体亲和力成熟](../../part-03-engineering-and-candidate-selection/06-antibody-engineering-and-optimization/affinity-maturation.md)
- [基准、泛化与能力边界](benchmarks-generalization-and-boundaries.md)

