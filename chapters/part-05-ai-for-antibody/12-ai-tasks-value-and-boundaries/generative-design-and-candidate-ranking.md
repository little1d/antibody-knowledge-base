---
title: 生成设计与候选排序
status: draft
last_reviewed: 2026-08-18
tags:
  - generative-design
  - candidate-ranking
  - multi-objective-optimization
lifecycle_stage:
  - discovery
  - engineering
ai_task:
  - sequence-generation
  - candidate-ranking
  - multi-objective-optimization
evidence_level: exploratory
---

# 生成设计与候选排序

## 对应的研发问题

生成式模型可以提出未在当前文库中出现的序列或结构；候选排序系统则在更大空间中决定哪些序列值得进入实验。研发价值不在“生成了多少”，而在于生成空间是否受正确约束、前排候选是否优于对照，以及实验结果能否反馈到下一轮。

## 生成不等于预测

- **生成任务**提出候选：哪些序列可能满足约束？
- **预测任务**评价候选：某个候选可能表现如何？
- **排序任务**分配预算：有限实验名额优先给谁？
- **优化任务**在多个性质之间寻找更好的权衡。

一个工作流可以同时包含四者，但每一步的错误会传递到后续。

## 常见生成约束

抗体序列生成通常需要同时考虑：

- 固定框架或胚系背景；
- 只改变指定 CDR 或界面位置；
- 保留关键表位和作用机制；
- 结构兼容和无明显空间冲突；
- 天然性、人源性和序列责任位点；
- 表达、稳定性、聚集和非特异性风险；
- 多样性，避免候选全部集中在同一模式。

约束越多，搜索越接近研发问题；但如果预测器本身不可靠，复杂约束也可能产生虚假的确定感。

## 从固定结构到序列设计

逆折叠方法在给定骨架下生成兼容序列。用于抗体–抗原复合物时，可以让模型看到界面几何并在指定位置提出替换。

这种方法依赖输入结构。固定骨架假设适合局部变化，对需要 CDR 大幅调整或诱导契合的设计可能过强。

## 序列与结构交替搜索

项目资料将 AffinityFlow 作为序列–结构交替搜索的例子：

1. 给定序列生成可能的复合物构象；
2. 使用预测信号引导结构向更有利方向变化；
3. 在更新后的结构上进行逆折叠设计；
4. 让序列和结构模型相互校验；
5. 重复迭代形成候选。

这类路线允许骨架和 CDR 局部变化，搜索空间更大，也更容易放大错误。如果初始复合物或评分函数错误，模型可能围绕错误界面持续优化。

本页将该方向标为 `exploratory`，直到逐篇核验前瞻性实验和跨体系结果。

## 候选排序

候选排序可以组合多个信号：

- PLM 或抗体模型的序列合理性；
- 逆折叠的结构兼容性；
- 复合物或能量模型的界面评分；
- 亲和力和突变效应模型；
- 表达、稳定性和非特异性风险；
- 与已有候选的多样性；
- 模型不确定性。

模型集成的作用不是把多个错误分数平均成真值，而是识别共识、分歧和风险。分歧候选可以进入小型定向文库或专门实验，而不是被简单丢弃。

## 多目标优化

抗体研发常同时关注：

- 亲和力或功能；
- 特异性；
- 表达和纯度；
- 稳定性和聚集；
- 人源性和免疫原性；
- 分子格式与工艺可行性。

把所有属性压成一个总分需要人为权重，也可能掩盖硬风险。另一种方法是保留 Pareto 前沿：位于前沿的候选无法在不牺牲其他属性的情况下继续改善某一属性。

最终选择仍需 TPP、硬门槛和项目判断。

## 多样性为什么重要

如果只选择预测最高的相似序列，可能出现：

- 全部候选共享同一模型偏差；
- 缺少用于判断位点和突变类型的对照；
- 实验数据集中在很小区域，难以训练下一轮模型；
- 过早收敛到局部最优。

候选集应在性能、风险、不确定性和序列/结构多样性之间平衡。

## 实验出口

生成候选进入实验前，应明确：

1. 生成范围和硬约束；
2. 每个过滤器的含义和适用范围；
3. 候选数量和对照设计；
4. 先测表达/结合还是完整功能；
5. 是否回到目标 IgG 或药物格式；
6. 失败结果如何记录并进入下一轮。

## 评价方式

- 有效生成率：序列是否合法、可制造和可表达；
- 新颖性与多样性：不是训练序列的简单复制；
- top-k 实验命中率；
- 相对随机、人工或传统物理方法的增益；
- 多属性和最终格式下的保持情况；
- 需要测试多少候选才能找到改善体。

“模型一次生成一万条序列”不是研发评价指标。

## 主要边界

- 评分器误差被生成器利用；
- 过度偏向天然分布，限制非天然工程空间；
- 结构和复合物错误的级联放大；
- 多目标标签稀缺且实验条件不一致；
- 生成序列可合成但不可表达、不可折叠或无功能；
- 缺少与合理对照的前瞻性比较。

## 关键结论

生成模型的价值是扩大和重组候选空间，候选排序的价值是把实验预算集中到更有信息量的序列。二者都必须受作用机制、实验真值和多属性门槛约束，并通过前瞻性实验评价。

## 延伸阅读

- [抗体结构与复合物预测](structure-and-complex-prediction.md)
- [抗体可开发性概览](../../part-03-engineering-and-candidate-selection/07-specificity-and-developability/developability-overview.md)
- [主动学习与 Lab-in-the-loop](active-learning-and-lab-in-the-loop.md)

